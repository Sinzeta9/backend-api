services:
  postgres:
    image: postgres:17
    container_name: postgres-local

    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}

    ports:
      - "5432:5432"

    volumes:
      - postgres-local_postgres_data:/var/lib/postgresql/data

    restart: unless-stopped

volumes:
  postgres-local_postgres_data:
    external: true